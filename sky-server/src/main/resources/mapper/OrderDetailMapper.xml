<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert into order_detail
        (name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount)
        values
        <foreach collection="list" item="od" separator=",">
            (#{od.name},#{od.image},#{od.orderId},#{od.dishId},
             #{od.setmealId},#{od.dishFlavor},#{od.number},#{od.amount})
        </foreach>
    </insert>

    <select id="getNameWithNumberByMap" resultType="com.sky.dto.GoodsSalesDTO">
        SELECT od.name as name, count(od.number) as number
        FROM `order_detail` as od,
             orders as o
        WHERE od.order_id = o.id
          and o.status = #{status}
          and o.order_time BETWEEN #{begin} and #{end}
        GROUP BY od.`name`
        order by number desc
        LIMIT 10
    </select>

</mapper>
